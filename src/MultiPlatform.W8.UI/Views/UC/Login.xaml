<UserControl
    x:Class="MultiPlatform.W8.UI.Views.UC.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultiPlatform.W8.UI.Views.UC"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:metroEventToCommand="using:MetroEventToCommand"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
       DataContext="{Binding Source={StaticResource Locator}, Path=Login}"
    >
    
    <Grid>
        <StackPanel>
            <Button   Command="{Binding DoLogin, Mode=OneWay}"  Content="{Binding Translation.Button_DoLogin, Mode=OneWay, Source={StaticResource LabelsManager}}">
               

            </Button>
        </StackPanel>
    </Grid>
</UserControl>
